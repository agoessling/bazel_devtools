load("@rules_shell//shell:sh_binary.bzl", "sh_binary")

package(default_visibility = ["//visibility:public"])

exports_files([
    "LICENSE",
    "README.md",
    "clang_tidy.bzl",
])

filegroup(
    name = "distribution_files",
    srcs = glob(["**"]),
)

sh_binary(
    name = "clang_tidy_filter",
    srcs = ["clang_tidy_filter.sh"],
)
